import React  from 'react';
import './styles.scss';
import PropTypes from 'prop-types';

const SideBar = (props) => {
return (
	<div>
		SideBar
	</div>
)
};

SideBar.propTypes={};

export default SideBar